import styled from 'styled-components'
// import moviesRoot from '../utils/request'

const Nav = () => {
    return (
        <Navbar>
            
        </Navbar>
    )
}

export default Nav

const Navbar = styled.nav`

`
